# EAS Build Ignore File
# Exclude large files and build artifacts to speed up uploads
# NOTE: Do NOT exclude ios/ or android/ folders for bare workflow projects!

# iOS - only exclude build artifacts, NOT the source files
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
ios/*.xcodeproj/project.xcworkspace/xcuserdata/
ios/DerivedData/

# Android - only exclude build artifacts, NOT the source files
android/build/
android/app/build/
android/.gradle/

# Node modules (already excluded by default, but be explicit)
node_modules/

# Git
.git/

# Misc
.DS_Store
*.log
