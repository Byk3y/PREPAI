UI sound effects live here.

- Keep each clip short (<300ms) and small (<50KB), mono WAV preferred.
- Suggested names to match the feedback helper: `tap`, `correct`, `incorrect`, `hint`, `complete`, `flip`.
- Avoid clipping; normalize to about -2 dB. If you need a placeholder, drop a silent WAV with the expected name.














